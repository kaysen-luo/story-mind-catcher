// MVS-013 · v1.0 · UI 折叠状态分组占位
// T1 阶段：leftCollapsed / rightCollapsed / loadCollapsedState /
// toggleLeftCollapsed / toggleRightCollapsed 保留在 app.js。T6 再拆到本文件。
// 注：v0.3.1 用 body.classList 双写状态（DOM class + Alpine state），
// review 建议 T6 清理，T1 不动。
export {};
